# Generated by `pnpm --filter @clickclack/mobile add:ios` / `add:android`.
# The native projects are reproducible from capacitor.config.ts plus
# scripts/configure-native.mjs, so they are not tracked here.
android/
ios/
