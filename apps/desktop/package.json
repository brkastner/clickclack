{
  "name": "@clickclack/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Native ClickClack desktop shell for macOS, Windows, and Linux",
  "homepage": "https://clickclack.chat",
  "license": "MIT",
  "author": "OpenClaw contributors",
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "rebuild:native": "electron-builder install-app-deps",
    "smoke:terminal": "pnpm run rebuild:native && node scripts/smoke-terminal.mjs",
    "smoke:terminal:isolation": "pnpm run build && node scripts/smoke-terminal-isolation.mjs",
    "smoke:terminal:packaged": "node scripts/smoke-terminal.mjs --packaged",
    "verify:pack": "pnpm run pack && pnpm run smoke:terminal:packaged",
    "dev": "pnpm run rebuild:native && pnpm build && electron .",
    "dist": "pnpm build && electron-builder --publish never",
    "dist:linux": "pnpm build && electron-builder --linux AppImage deb --x64 --publish never",
    "dist:mac": "pnpm build && electron-builder --mac dmg zip --x64 --arm64 --publish never",
    "dist:mac:release": "bash scripts/package-macos-release.sh",
    "dist:win": "pnpm build && electron-builder --win nsis zip --x64 --publish never",
    "pack": "pnpm build && electron-builder --dir --publish never",
    "test": "node scripts/test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0",
    "node-pty": "1.1.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "electron": "43.3.0",
    "electron-builder": "26.15.3",
    "esbuild": "0.28.2"
  },
  "desktopName": "clickclack.desktop"
}
